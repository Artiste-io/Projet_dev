{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <div style="margin-left: 5%;">
        {{ form_start(registrationForm) }}
        {#        {{ form_row(registrationForm.name) }}#}
        {#        {{ form_row(registrationForm.email) }}#}
        {#        {{ form_row(registrationForm.plainPassword, {#}
        {#            label: 'Password'#}
        {#        }) }}#}
        {#        {{ form_row(registrationForm.agreeTerms) }}#}

        <br>
        <h1 class="h3 mb-3 font-weight-normal">Please register</h1>

        <label for="formGroupExampleInput" class="form-label">{{ form_row(registrationForm.username) }}</label>
        <label for="formGroupExampleInput" class="form-label">{{ form_row(registrationForm.profil) }}</label>
        <label for="formGroupExampleInput" class="form-label">{{ form_row(registrationForm.photo) }}</label>
        <label for="formGroupExampleInput" class="form-label">{{ form_row(registrationForm.email) }}</label>
        <label for="formGroupExampleInput" class="form-label">{{ form_row(registrationForm.plainPassword, {
                label: 'Password'
            }) }}
        </label>

        {{ form_row(registrationForm.agreeTerms) }}
        <button type="submit" class="btn btn-primary mb-3">Register</button>
        {{ form_end(registrationForm) }}
    </div>
    <div style="margin-left: 5%;">
        <h1 class="h3 mb-3 font-weight-normal">You already have an account ?</h1>
        <a class="btn btn-primary mb-3"href="{{ path('app_login') }}">Login</a>
    </div>
{% endblock %}